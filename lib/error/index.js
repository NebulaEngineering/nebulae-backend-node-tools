"use strict";

const { CustomError, INTERNAL_SERVER_ERROR_CODE, PERMISSION_DENIED } = require("./CustomError");

module.exports = {
    CustomError,
    INTERNAL_SERVER_ERROR_CODE,
    PERMISSION_DENIED
};
